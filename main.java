public class main {
    public static void main(String[] args) {
        // Crée une instance de Fraction
        fraction fraction = new fraction();

        // Appelle la méthode toString et affiche le résultat
        System.out.println(fraction.toString());  // Cela affichera "Je suis une fraction."
    }
}
